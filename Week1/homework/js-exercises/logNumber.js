'use strict'
let numberX;
console.log(the value of x undefined. Because there is no value defined);
console.log(numberX);
numberX = 2;
console.log(I initialized the value and the value of x should be 2 );
console.log(numberX);